source 'https://rubygems.org'

# Mirrors the Jekyll + plugins set that GitHub Pages uses.
# Enables local builds via `bundle exec jekyll build` or `bundle exec jekyll serve`.
gem 'github-pages', group: :jekyll_plugins
